To build calculator cd to current, then input in terminal:

go build calculator.go main.go

./calculator

Note! In the top left corner will be menu that allows to convert numbers to different base system and keep calculating in them